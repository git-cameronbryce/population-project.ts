export interface AscendedProps {
  MaxPlayers: number;
  NumPlayers: number;
  MapName: string;
  Name: string;
  IP: string;
}

export interface EvolvedProps {
  MaxPlayers: number;
  NumPlayers: number;
  MapName: string;
  Name: string;
  IP: string;
}

export interface InputProps {
  cluster: string;
}